# ytmpd i3 keybindings
#
# Add these to your i3 config file (usually ~/.config/i3/config)
# Adjust the modifier key ($mod) and key combinations as needed.
#
# To use these bindings:
# 1. Make sure ytmpd daemon is running
# 2. Add the bindings below to your i3 config
# 3. Reload i3 config (mod+Shift+r)

# Play/pause toggle (requires custom script or use separate bindings)
# Note: ytmpd doesn't have a toggle command, so use pause/resume separately

# Pause playback
bindsym $mod+Shift+p exec --no-startup-id /path/to/ytmpd/bin/ytmpctl pause

# Resume playback
bindsym $mod+Shift+r exec --no-startup-id /path/to/ytmpd/bin/ytmpctl resume

# Stop playback
bindsym $mod+Shift+s exec --no-startup-id /path/to/ytmpd/bin/ytmpctl stop

# Next song
bindsym $mod+Shift+n exec --no-startup-id /path/to/ytmpd/bin/ytmpctl next

# Previous song
bindsym $mod+Shift+b exec --no-startup-id /path/to/ytmpd/bin/ytmpctl prev

# Show current status in notification (requires notify-send)
bindsym $mod+Shift+i exec --no-startup-id notify-send "ytmpd" "$(/path/to/ytmpd/bin/ytmpctl status)"

# Search and play (using dmenu or rofi)
# This example uses dmenu - adjust for rofi if preferred
bindsym $mod+Shift+m exec --no-startup-id /path/to/ytmpd/bin/ytmpctl play "$(echo '' | dmenu -p 'Play song:')"

# Alternative keybindings using function keys (F7-F10)
# Uncomment if you prefer using function keys:

# bindsym F7 exec --no-startup-id /path/to/ytmpd/bin/ytmpctl prev
# bindsym F8 exec --no-startup-id /path/to/ytmpd/bin/ytmpctl pause
# bindsym F9 exec --no-startup-id /path/to/ytmpd/bin/ytmpctl resume
# bindsym F10 exec --no-startup-id /path/to/ytmpd/bin/ytmpctl next

# Media key bindings (if your keyboard has media keys)
# Note: XF86Audio* keys are standard media key names

# bindsym XF86AudioPlay exec --no-startup-id /path/to/ytmpd/bin/ytmpctl resume
# bindsym XF86AudioPause exec --no-startup-id /path/to/ytmpd/bin/ytmpctl pause
# bindsym XF86AudioNext exec --no-startup-id /path/to/ytmpd/bin/ytmpctl next
# bindsym XF86AudioPrev exec --no-startup-id /path/to/ytmpd/bin/ytmpctl prev
# bindsym XF86AudioStop exec --no-startup-id /path/to/ytmpd/bin/ytmpctl stop

# ===== Radio and Search Features =====

# Generate radio playlist from currently playing track
bindsym $mod+Shift+r exec --no-startup-id /path/to/ytmpd/bin/ytmpctl radio

# Generate radio and auto-apply (clear queue, load radio, play)
bindsym $mod+Shift+Alt+r exec --no-startup-id /path/to/ytmpd/bin/ytmpctl radio --apply

# Open interactive search in terminal
# Requires a terminal emulator - adjust 'alacritty' to your preferred terminal
bindsym $mod+Shift+f exec --no-startup-id alacritty -e /path/to/ytmpd/bin/ytmpctl search
